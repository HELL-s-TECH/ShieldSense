fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
rapidfuzz==3.9.7
tldextract==5.1.2
requests==2.34.2  # Google Safe Browsing reputation lookup (optional, needs a key)

# text classifier (runtime: loading + inference)
scikit-learn==1.9.0
joblib==1.5.3

# RAG/LLM reasoning tier (only used when a key is configured)
anthropic==1.0.0
openai==3.3.1  # used for xAI/Grok — its API is OpenAI-compatible

# auth
bcrypt==5.0.0
PyJWT==2.9.0

# text classifier (training only — not needed to run the API)
pandas==3.0.5
kagglehub==0.3.13
